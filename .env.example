# Runtime
PORT=5173

# Storage
DB_PATH=./data/pmg.sqlite

# Embeddings + LLM
EMBEDDINGS_PROVIDER=mock   # mock | openai
LLM_PROVIDER=mock          # mock | openai
OPENAI_API_KEY=            # set only when using OpenAI

# Privacy / multi-user: here we keep a simple header-based user id for now
# In production, put real auth in front of PMG.
DEFAULT_USER_ID=demo-user
